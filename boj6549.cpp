#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0); cin.tie(0);
    stack<int> s;
    int n=1;
    cin >> n;
    while(n!=0){
        int max=0;
        for(int i=0;i<n;i++){
            int tmp;
            cin >> tmp;
            
            s.push(tmp);
        }
    }
}
